import LoginForm from '../components/LoginForm';

// TODO: Combine login and register page into one. If the user is already logged in, have it display a page saying so
const LoginPage = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default LoginPage; 